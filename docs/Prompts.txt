okay now we need to start working on building the inventory and procurement module as part of Rahah24, so we need to look at the plan in detail. We should not create all the database tables and split inventory management into phases, as we go about development and testing of this system, we can add tables in the database step by step as if we add all the tables now in supabase, it will not work as expected. What I have now is a plan as to how to start, we need to design forms, the users required so approval workflows can work as expected. Remember and you can mention this in Claude.md file, that all user emails will be on the domain rahah24.com, so currently there is only the admin defined admin@rahah24.com, this guy has access to all the modules say executive KPI, business dashboards, financials, HR, Islamic modules and so on. 

